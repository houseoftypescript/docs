extend type Query {
  competitions: [Competition]
  competition(id: String!): Competition
}

type Competition {
  id: Tier
  name: String
}

enum Tier {
  TIER_ONE
  TIER_TWO
  TIER_THREE
  TIER_CUP
}
