query GET_TEAM_WITH_PLAYERS($teamId: String!) {
  team(id: $teamId) {
    id
    name
    fullName
    stadiumId
    chairman
    manager
    founded
    active
    competitionId
    players {
      id
      positionId
      shirtNumber
      fullName
      dateOfBirth
    }
  }
}
